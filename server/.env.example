# Порт приложения (HTTP + WebSocket). Порт БД задаётся только в DATABASE_URL.
SERVER_PORT=3000
PORT=3000

DATABASE_URL=postgresql://tanks:tanks@localhost:5432/tanks2d
JWT_SECRET=замените_на_длинную_случайную_строку_не_короче_32_символов

# Скопируйте в .env. Файл .env в git не коммитится.
# Схема БД: из каталога server выполните npm run migrate
